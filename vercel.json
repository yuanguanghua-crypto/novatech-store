{
  "framework": "nextjs",
  "buildCommand": "prisma db push --accept-data-loss --skip-generate && prisma generate && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "regions": ["iad1"]
}
