---
name: openspec-continue
description: Create the next artifact for the current change
metadata:
  author: openspec
  generatedBy: "manual-setup"
---

This is an OpenSpec expanded workflow skill for the novatech-store project.

See `openspec/config.yaml` for project context and constraints.

When triggered (when the user wants to continue building the current change), provide a reference to the corresponding prompt in `~/.codex/prompts/opsx-continue.md`.
