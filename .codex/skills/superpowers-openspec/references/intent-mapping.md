# 中文意图到命令的映射

| 用户表达 | 处理方式 |
|----------|----------|
| "先沟通需求，再做功能" | 先 `/opsx:explore` |
| "写方案文档" | 先 `docs/solutions/*.md`，确认后进入 `/opsx:new` |
| "定方案和开发计划" | `/opsx:new` + `/opsx:continue` 分步确认 |
| "规划好了，开始实现" | `/opsx:apply` |
| "检查实现是否偏离" | `/opsx:verify` |
| "做完了，归档" | `/opsx:archive` |
