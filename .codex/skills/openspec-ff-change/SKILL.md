---
name: openspec-ff
description: Fast-forward all planning artifacts for a change
metadata:
  author: openspec
  generatedBy: "manual-setup"
---

This is an OpenSpec expanded workflow skill for the novatech-store project.

See `openspec/config.yaml` for project context and constraints.

When triggered (when the user wants to generate all planning artifacts at once), provide a reference to the corresponding prompt in `~/.codex/prompts/opsx-ff.md`.
