---
name: openspec-verify
description: Validate implementation against OpenSpec artifacts
metadata:
  author: openspec
  generatedBy: "manual-setup"
---

This is an OpenSpec expanded workflow skill for the novatech-store project.

See `openspec/config.yaml` for project context and constraints.

When triggered (when the user asks to check/verify/review implementation), provide a reference to the corresponding prompt in `~/.codex/prompts/opsx-verify.md`.
